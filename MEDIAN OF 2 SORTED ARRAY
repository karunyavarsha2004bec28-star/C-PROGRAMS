#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))

double findMedianSortedArrays(int* nums1, int nums1Size, int* nums2, int nums2Size) {
    
    if (nums1Size > nums2Size) {
        return findMedianSortedArrays(nums2, nums2Size, nums1, nums1Size);
    }

    int m = nums1Size;
    int n = nums2Size;
    int low = 0, high = m;
    int total_left = (m + n + 1) / 2;

    while (low <= high) {
        int i = low + (high - low) / 2; 
        int j = total_left - i;         

       
        int maxLeft1 = (i == 0) ? -1e7 : nums1[i - 1];
        int minRight1 = (i == m) ? 1e7 : nums1[i];

        int maxLeft2 = (j == 0) ? -1e7 : nums2[j - 1];
        int minRight2 = (j == n) ? 1e7 : nums2[j];

        if (maxLeft1 <= minRight2 && maxLeft2 <= minRight1) {
           
            if ((m + n) % 2 != 0) {
                return (double)MAX(maxLeft1, maxLeft2);
            }
            return ((double)MAX(maxLeft1, maxLeft2) + MIN(minRight1, minRight2)) / 2.0;
        } 
        else if (maxLeft1 > minRight2) {
        
            high = i - 1;
        } 
        else {
            
            low = i + 1;
        }
    }

    return 0.0;
}
